function sum(a: number, b: number): number {
  return a + b;
}

function rest(a: number, b: number): number {
  return a - b;
}
const x: number = 5;
const y: number = 10;

console.log(`La suma es: ${sum(x, y)}`);
