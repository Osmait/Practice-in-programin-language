// Operaciones matemáticas básicas
const sum2 = 5 + 3;  // Suma
const difference = 5 - 3;  // Resta
const product = 5 * 3;  // Multiplicación
const quotient = 5 / 3;  // División
const remainder = 5 % 3;  // Resto (módulo)
const exponentiation = 5 ** 3;  // Potenciación
console.log(exponentiation)

// Funciones matemáticas incorporadas
const squareRoot = Math.sqrt(16);  // Raíz cuadrada
const exponential = Math.exp(2);  // Exponencial (e^2)
const naturalLogarithm = Math.log(10);  // Logaritmo natural (base e)
const base10Logarithm = Math.log10(100);  // Logaritmo en base 10
const sine = Math.sin(Math.PI / 6);  // Seno
const cosine = Math.cos(Math.PI / 3);  // Coseno
const tangent = Math.tan(Math.PI / 4);  // Tangente

// Redondeo
const rounded = Math.round(3.14159265);  // Redondear
const ceiling = Math.ceil(4.1);  // Redondear hacia arriba
const floor = Math.floor(4.9);  // Redondear hacia abajo

// Valor absoluto
const absoluteValue = Math.abs(-5);

// Comparaciones
const maximum = Math.max(3, 7, 1, 9);  // Valor máximo
const minimum = Math.min(3, 7, 1, 9);  // Valor mínimo

// Generación de números aleatorios
const randomDecimal = Math.random();  // Número decimal aleatorio entre 0 y 1
const randomInteger = Math.floor(Math.random() * 10) + 1;  // Número entero aleatorio entre 1 y 10
